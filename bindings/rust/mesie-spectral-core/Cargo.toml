[package]
name = "mesie-spectral-core"
version = "1.0.0"
edition = "2021"
description = "MESIE Rust runtime — validate and match spectral JSON"

[[bin]]
name = "mesie-spectral-core"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"