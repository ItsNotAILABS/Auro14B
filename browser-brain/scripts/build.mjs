import {build} from 'esbuild';import {cp,mkdir} from 'node:fs/promises';import {execFileSync} from 'node:child_process';execFileSync('python',['scripts/export_browser_weights.py'],{stdio:'inherit'});await mkdir('dist',{recursive:true});for(const name of ['manifest.json','sidepanel.html','sidepanel.css'])await cp(`extension/${name}`,`dist/${name}`);await mkdir('dist/models/HIM-native-v0',{recursive:true});await cp('models/HIM-native-v0','dist/models/HIM-native-v0',{recursive:true});await mkdir('dist/vendor/transformers',{recursive:true});for(const name of ['ort-wasm-simd-threaded.jsep.wasm','ort-wasm-simd-threaded.jsep.mjs'])await cp(`node_modules/@huggingface/transformers/dist/${name}`,`dist/vendor/transformers/${name}`);await build({entryPoints:['extension/background.js'],bundle:true,format:'esm',platform:'browser',outfile:'dist/background.bundle.js',minify:true,sourcemap:false});await build({entryPoints:['extension/sidepanel.js'],bundle:true,format:'iife',platform:'browser',outfile:'dist/sidepanel.bundle.js',minify:true,sourcemap:false});console.log('HIM_CHROME_EXTENSION_BUILT dist/');
