{
  "name": "nexus-relay",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/extract.js && node --check src/mcp.js && node --check src/auth.js && node --check src/skill.js && node --check scripts/issue-key.mjs"
  },
  "engines": {"node": ">=20"}
}
