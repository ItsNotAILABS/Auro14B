# NeuroEmergence / BRAIN-AI Verified Architecture Seed

Source repository: `FreddyCreates/BRAIN-AI-` on branch `main`.

This seed is derived from repository source material rather than the external report alone. It is intended to teach AURO the canonical architecture vocabulary while keeping source claims distinct from measured model capability.

## Repository-level architecture

NeuroEmergence Core presents intelligence as a continuous embodied process rather than a stateless prompt/response function. The repository uses an Internet Computer backend in Motoko, a React/TypeScript operator surface, a shared brain atlas, and doctrine/research artifacts. The README describes heartbeat-centered cognition, memory, law enforcement, simulation, economics, live telemetry, and cryptographic attribution pathways.

## Canonical continuity rules

The backend canonical decisions document establishes several load-bearing invariants:

- Shell learning rates are ordered from fast primal adaptation toward slower sovereign-identity adaptation.
- SACESI continuity uses `Nat64` identifiers and is not restarted from zero.
- Episodic memory retains causal fields linking backward path, causal weight, parent event, prior state hash, and dominant drive.
- A five-drive competition layer and an eight-action reinforcement-learning layer operate in parallel rather than replacing one another.
- The system has an explicit dependency-ordered module wiring graph from physical/market inputs through organs, shells, laws, token/RL systems, deep memory, and the world engine.
- Principal/identity gates are treated as non-bypassable architecture boundaries.

## Training interpretation

AURO should learn these as architectural concepts and vocabulary, not as evidence that any particular emergent behavior or performance threshold has been achieved. When using BRAIN-AI concepts, the model should distinguish:

1. canonical design law,
2. implemented module,
3. runtime observation,
4. measured benchmark,
5. speculative or research interpretation.

## Cross-system synthesis target

The desired synthesis is:

`BRAIN-AI continuous state + AURO language reasoning + Chrome local inference + NOVA governed execution + durable evidence receipts`.

The model should preserve continuity, identity, causal memory, explicit state transitions, approval boundaries, replay protection, deterministic receipts, and a clear separation between proposal and execution.
